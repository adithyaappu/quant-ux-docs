<template><div><h1 id="create-your-first-prototype" tabindex="-1"><a class="header-anchor" href="#create-your-first-prototype"><span>Create Your First Prototype</span></a></h1>
<h2 id="quick-demo" tabindex="-1"><a class="header-anchor" href="#quick-demo"><span>Quick Demo</span></a></h2>
<VidStack load="idle" src="/assets/videos/1-your-first-prototype.webm" /><h2 id="detailed-steps" tabindex="-1"><a class="header-anchor" href="#detailed-steps"><span>Detailed Steps</span></a></h2>
<h3 id="design" tabindex="-1"><a class="header-anchor" href="#design"><span>Design</span></a></h3>
<p>After signing in into your account,</p>
<ol>
<li>Click on the <strong><code v-pre>+ Create</code></strong> button in the left panel</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/3-create-app-button.png" alt="create app button highlight"></p>
<ol start="2">
<li>In the dialogue box, first enter a name. Then either select a screen size or enter custom values as required</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/4-create-app-dialogue.png" alt="create app dialogue"></p>
<ol start="3">
<li>The app designer will open up. In this, select the <strong><code v-pre>Add Screen (s)</code></strong> button, to insert a screen into the canvas</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/5-add-screen-button.png" alt="add screen button highlight"></p>
<ol start="4">
<li>A new screen with the previously selected (Step 2) dimensions will get added</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/6-screen-added.png" alt="new screen in canvas"></p>
<ol start="5">
<li>Click on <code v-pre>Add Basic Elements</code> button, and select <code v-pre>Text (T)</code>. Click and drag to add a text box.</li>
</ol>
<div class="hint-container important">
<p class="hint-container-title">Important</p>
<p>Note that the height of the text box will decide the font size.</p>
</div>
<p><img src="@source/content/getting-started/media/pictures/7-add-basic-elements-type.png" alt="selecting type tool"></p>
<div class="hint-container tip">
<p class="hint-container-title">Tips</p>
<p>You can simply press <code v-pre>T</code> in your keyboard to activate the type tool.<br>
Similarly look for shortcuts given with all the tools.</p>
</div>
<ol start="6">
<li>Type in a text you want. For example &quot;My App&quot;.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/8-text-added.png" alt="text added into the screen"></p>
<ol start="7">
<li>Click on the <code v-pre>Add Interactive Widgets (W)</code> button</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/9-widget-button.png" alt="button to add widgets"></p>
<ol start="9">
<li>Add a <code v-pre>Primary Button</code> widget into your screen</li>
</ol>
<div class="hint-container important">
<p class="hint-container-title">Important</p>
<p>When clicked on a widget, an instance of that widget will be added to the screen, but this instance will be attached to the mouse pointer. Move the widget to the desired location and click again to fix the position.</p>
</div>
<p><img src="@source/content/getting-started/media/pictures/10-widget-window.png" alt="widget window"></p>
<ol start="10">
<li>Add another screen repeating the same steps used to create the first one.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/11-button-and-screen-added.png" alt="new screen added"></p>
<div class="hint-container tip">
<p class="hint-container-title">Tips</p>
<p>You have hold <code v-pre>spacebar</code> in your keyboard and move the <code v-pre>mouse</code> to pan in the canvas.</p>
</div>
<ol start="11">
<li>Add a text and a <code v-pre>Back</code> button, to the second screen. <code v-pre>Double Click</code> on the button to edit the label to &quot;Back&quot;.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/12-text-and-button-added.png" alt="added new text and button"></p>
<h3 id="prototype" tabindex="-1"><a class="header-anchor" href="#prototype"><span>Prototype</span></a></h3>
<ol start="12">
<li>Click on the <code v-pre>Prototype</code> switch in the right sidebar to enter into the prototype mode.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/13-prototype-mode.png" alt="prototype mode"></p>
<ol start="13">
<li>Click on the button in the first screen. A <code v-pre>Handle</code> will be displayed on the middle of the right edge.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/14-prototype-handle.png" alt="prototype handle"></p>
<div class="hint-container tip">
<p class="hint-container-title">Tips</p>
<p>You can hold <code v-pre>Ctrl</code> or <code v-pre>Cmd</code> on your keyboard and scroll your <code v-pre>Mouse Up</code> and <code v-pre>Mouse Down</code> to zoom in and zoom out of the canvas, respectively.</p>
</div>
<ol start="14">
<li>Click on the <code v-pre>Handle</code> once so that a <code v-pre>Wire</code> will get attached to the mouse pointer. Move the mouse to the second screen and click inside the screen one more time to release the <code v-pre>Wire</code> and finalise the connection.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/15-add-link-to-other-screen.png" alt="connector added"></p>
<ol start="15">
<li>Similarly add another connection from back button in the second screen to the first screen.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/16-add-back-link.png" alt="connection from back button"></p>
<h3 id="simulate" tabindex="-1"><a class="header-anchor" href="#simulate"><span>Simulate</span></a></h3>
<p>With all the <code v-pre>Wire</code>s in place, we can simulate the app.</p>
<ol start="16">
<li>Click on the <code v-pre>Start Simulation</code> button to launch the app in simulation mode.</li>
</ol>
<p><img src="@source/content/getting-started/media/pictures/17-sim-mode.png" alt="simulation mode"></p>
<ol start="17">
<li>Click on the buttons to navigate between the screens.</li>
</ol>
<div class="hint-container info">
<p class="hint-container-title">Info</p>
<p>Congratulations! First part of your prototype is done. Head over to next section to learn about Testing</p>
</div>
</div></template>


